<% @items.each do |item| %>

  <h5>Item id: <%= item.id %></h5>
  <ul>
    <li><%= item.name %></li>
    <li>Price: $<%= item.price %></li>
    <li><%= image_tag(item.image, class: :thumb) %></li>
    <li>In stock: <%= item.instock_qty %></li>
  </ul>

  <%= button_to("Edit Item", dashboard_item_edit_path(item)) %>
<% end %>
